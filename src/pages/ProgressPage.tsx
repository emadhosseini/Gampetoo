// Placeholder — the bottom nav slot this occupies was freed up by merging
// the workout/nutrition tabs into DailyProgramPage. Real content (charts,
// history, etc.) comes later.
export default function ProgressPage() {
  return (
    <div className="flex min-h-[calc(100vh-6rem)] flex-col items-center justify-center px-5 text-center">
      <h1 className="text-2xl font-bold text-white">پیشرفت</h1>

      <p className="mt-3 text-white">این بخش به‌زودی اضافه می‌شود.</p>
    </div>
  );
}